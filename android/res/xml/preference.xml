<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen
  xmlns:android="http://schemas.android.com/apk/res/android"
  android:title="Preferences">
  <CheckBoxPreference
    android:title="Enable Checkin"
    android:defaultValue="true"
    android:summary="Automatically fetch and push experiments?"
    android:key="@string/checkinEnabledPrefKey" />
  <EditTextPreference
    android:title="Set Checkin Interval"
    android:defaultValue="12"
    android:inputType="number"
    android:summary="The interval (hours) speedometer fetches and pushes measurements"
    android:key="@string/checkinIntervalPrefKey" />    
  <EditTextPreference
    android:title="Set Battery Threshold"
    android:defaultValue="60"
    android:inputType="number"
    android:summary="The minimum battery percentage to pause all measurements"
    android:key="@string/batteryCapPrefKey" />
  <CheckBoxPreference
    android:title="Measure When Plugged"
    android:defaultValue="true"
    android:summary="Automatically download and upload experiments when the phone is plugged?"
    android:key="@string/measureWhenPluggedPrefKey" />    
  <CheckBoxPreference
    android:title="Start on Boot"
    android:defaultValue="true"
    android:summary="Start Speedometer automatically when the phone boots?"
    android:key="@string/startOnBootPrefKey" />
</PreferenceScreen>
